import React from 'react';
import IconButton from '@app/components/general/IconButton/IconButton';

export default function OrderingOptions(): JSX.Element {
  return (
    <>
      <IconButton name="cart" onPress={() => {}} />
    </>
  );
}
